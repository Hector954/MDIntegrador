<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dessof.conjuntos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/index"
        android:label="Proyecto Integrador"
        android:roundIcon="@mipmap/index"
        android:supportsRtl="true"
        android:theme="@style/Theme.Conjuntos">
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".inicio" android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>